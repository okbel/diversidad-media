type Movie {
  id: ID!
  title: String
}

type Show {
  id: ID!
  title: String
}

type Book {
  id: ID!
  title: String
}

type RootQuery {
  test: String
  movie: Movie
  show: Show
  book: Book
}

schema {
  query: RootQuery
}